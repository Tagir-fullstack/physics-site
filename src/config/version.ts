/**
 * Site Version Tracking
 *
 * Version format: vX.Y.Z.W
 *   X = Global site version (0 = pre-production, 1 = production release)
 *   Y = Significant features added (1 = quiz system added)
 *   Z = Number of animations on the site (9 animations total)
 *   W = Minor changes and fixes (2)
 *
 * When making changes, update the version here and add a corresponding
 * entry to the Changelog page (src/pages/Changelog.tsx).
 */
export const SITE_VERSION = 'v1.1.9.5';
